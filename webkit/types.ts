export type FreeGame = {
  appid: number;
  name: string;
};

export type GrabbedItem = {
  appid: number;
  added: boolean;
};

export type PanelSide = 'left' | 'right';
export type TabStyle  = 'slim' | 'large' | 'floating';
